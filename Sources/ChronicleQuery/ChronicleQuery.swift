/// The Chronicle query engine.
///
/// Parses human time ranges and search expressions into ``EventQuery`` values and
/// assembles timelines, statistics, and session narratives from repository
/// results. Depends on `ChronicleCore` and `ChronicleStorage`.
///
/// Implemented in Phases 6 and 7.
public enum ChronicleQuery {}
